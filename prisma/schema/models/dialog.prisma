model Dialog {
  id           Int                 @id @default(autoincrement())
  ownerId      Int                 @map("owner_id")
  title        String?             @db.VarChar(80)
  users        User[]
  Messages     Message[]
  blockedUsers DialogBlockedUser[]

  @@map("dialogs")
}
